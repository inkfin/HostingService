interface:
  display_name: "个人 VPS 部署与运维"
  short_description: "完成 HostingService 部署、服务增删与升级，验证 Gitea 恢复和定时备份"
  default_prompt: "使用 $hostingservice-deploy 完成本次 VPS 部署或运维需求，保留已有配置，只补齐缺项并验证受影响服务及备份。"
policy:
  allow_implicit_invocation: true
