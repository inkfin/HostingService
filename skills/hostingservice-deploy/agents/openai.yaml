interface:
  display_name: "个人 VPS 部署向导"
  short_description: "引导完成 HostingService 配置、Gitea 恢复验证和服务器定时异地备份"
  default_prompt: "使用 $hostingservice-deploy 带我完成 VPS 服务部署，逐项收集缺失配置，验证服务与备份恢复后再交付。"
policy:
  allow_implicit_invocation: true
