{
  "repository": "s3:https://REPLACE_ENDPOINT/REPLACE_BUCKET/hostingservice",
  "password_file": "runtime/restic-password",
  "env": {
    "AWS_ACCESS_KEY_ID": "REPLACE_ACCESS_KEY_ID",
    "AWS_SECRET_ACCESS_KEY": "REPLACE_SECRET_ACCESS_KEY",
    "AWS_DEFAULT_REGION": "REPLACE_REGION"
  }
}
